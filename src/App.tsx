import './App.css'
import Button from './components/button'

function App() {
  return (
    <>
     <Button bgColor={'red'} buttonText={'Red'} />
     <Button bgColor={'blue'} buttonText={'Blue'}/>
     <Button bgColor={'pink'} buttonText={'Pink'}/>
     <Button bgColor={'green'} buttonText={'Green'}/>
     <Button bgColor={'yellow'} buttonText={'Yellow'}/>
     <Button bgColor={'brown'} buttonText={'Brown'}/>

    </>
  )
}

export default App
